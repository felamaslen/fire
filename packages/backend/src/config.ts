/** ISO-4217 code of the home currency: the single currency all aggregate totals (`NetWorthEntry.totalAssets`, etc.) are expressed in, and the default used when no currency is specified on a client input. */
export const HOME_CURRENCY = "GBP";
