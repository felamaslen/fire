# fire

Track your net worth.

## Packages

- [`backend`](./packages/backend) — GraphQL API (Fastify + Apollo, schema generated by Grats)

## Getting started

```bash
pnpm install
pnpm dev
```

The API is served at `http://localhost:4000/graphql`.
